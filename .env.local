# File: .env.local
# REFACTOR / OVERWRITE
# NEXT_PUBLIC_API_URL is now set by start.js from ports.ini when running `npm run start:all`
# Remove the line below or comment it out if you sometimes run `npm run dev` directly
# NEXT_PUBLIC_API_URL=http://127.0.0.1:5000

# Other environment variables can remain here if needed